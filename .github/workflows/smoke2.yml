name: Publish isolated candidate campaign
on:
  push:
    branches: [main]
    paths:
      - .smoke2-trigger
permissions:
  contents: write
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: bash .payload/materialize.sh
