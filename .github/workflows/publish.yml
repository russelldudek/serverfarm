name: Stage candidate campaign payload

on:
  workflow_dispatch:

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload staging only. Publication is triggered after all parts are present."
